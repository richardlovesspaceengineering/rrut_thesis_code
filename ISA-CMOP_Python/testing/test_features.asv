% Testing the NDSort Function

%% Preamble
clc;
clear;

%% Creating data structures.
pop_global.obj = readmatrix("global/obj.csv");
pop_global.cv = readmatrix("global/cv.csv");
pop_global.rank_cons_rrut = readmatrix("global/rank_cons.csv");
pop_global.rank_uncons_rrut = readmatrix("global/rank_uncons.csv");

%% Checking NDSort

rank_cons_alsouly = NDSort(obj, cv, length(obj(:,1)))';
rank_uncons_alsouly = NDSort(obj, length(obj(:,1)))';

% Check equal
cons_equal = all(rank_cons_alsouly == rank_cons_rrut);
uncons_equal = all(rank_uncons_alsouly==rank_uncons_rrut);

%% Checking fsr calculation
